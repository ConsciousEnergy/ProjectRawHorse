# Development Dockerfile for Frontend
# Node.js with hot reload

FROM node:20-alpine

WORKDIR /app

# Copy package files
COPY package*.json ./

# Install dependencies
RUN npm ci

# No need to copy source - it will be mounted as a volume

EXPOSE 3000

CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]
